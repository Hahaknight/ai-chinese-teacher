# Server Configuration
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL="file:./dev.db"

# WeChat AppID and Secret
WECHAT_APP_ID=your_appid
WECHAT_APP_SECRET=your_appsecret

# Tencent Cloud COS (生产环境启用)
COS_SECRET_ID=your_secret_id
COS_SECRET_KEY=your_secret_key
COS_BUCKET=ai-chinese-teacher
COS_REGION=ap-guangzhou
COS_ENABLED=false

# minimax 平台(主 AI 客户端)
MINIMAX_API_KEY=your_minimax_api_key
MINIMAX_BASE_URL=https://api.minimaxi.com/v1
MINIMAX_API_HOST=https://api.minimaxi.com
MINIMAX_MODEL=MiniMax-M2.7
MINIMAX_FALLBACK_MODEL=MiniMax-M2.7

# 是否走两步流水线(每张图片独立 OCR,再合并批改)
# 1: 两步流水线 chat/completions + image_url(**已知 M2.7 在该端点不接受图片,会返空,不要开**)
# 0: 一步调用 /coding_plan/vlm(默认,2026-06-08 实测 M2.7 能正常 OCR 真实学生作文图)
MINIMAX_DIRECT=0

# 兼容旧配置
ANTHROPIC_API_KEY=your_minimax_api_key

# JWT
JWT_SECRET=your_jwt_secret_key_here

# 服务公开 base URL(部署到服务器时改成 https://your-domain.com)
PUBLIC_BASE_URL=

# 是否信任反向代理转发的 X-Forwarded-* / Host 头
# 1: 信任(部署了 nginx / caddy 等反代时开启)
# 0: 不信任(默认,避免被伪造 host 头影响 fileUrl 生成)
TRUST_PROXY=0